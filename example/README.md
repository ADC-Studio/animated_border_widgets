# animated_border_widgets_example

Small demo app for `animated_border_widgets`.

Run:

```bash
flutter pub get
flutter run
```

Shows:

- Tap Boost button
- Hold Boost button
- Hold to reveal animated border
- Demo bottom bar with boost on tap
