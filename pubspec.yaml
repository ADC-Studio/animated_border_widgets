name: animated_border_widgets
description: Reusable animated gradient border widgets for Flutter UIs.
version: 0.1.0
homepage: https://github.com/mixkage/animated_border_widgets

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
